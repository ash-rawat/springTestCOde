peline{
    agent {
        label 'vm2'
    }
    stages{
        stage('Maven Build'){
            steps{
                sh "echo Adarsh"
                // sh "mvn clean install"
            }
        }
    }
}
